name: Build Zig Bindings Windows

on:
  workflow_dispatch:
  workflow_call:

jobs:
  build-windows:
    runs-on: WIN-BUILD

    steps:
      - name: Checkout ngixi-builds repo
        uses: actions/checkout@v4

      - name: Stub Zig bindings build
        shell: pwsh
        run: |
          Write-Host "Zig bindings build stub - TODO: implement actual build"
          New-Item -ItemType Directory -Path zig-bindings-assets -Force
          # Create placeholder to ensure artifact uploads
          "STUB: Zig bindings not yet implemented" | Out-File -Encoding utf8 zig-bindings-assets/README.md
          # TODO: Add actual Zig bindings generation steps here

      - name: Upload Zig bindings artifacts
        uses: actions/upload-artifact@v4
        with:
          name: zig-bindings-windows-assets
          path: zig-bindings-assets/